
<h1>Items inactive</h1>

<div id="items" class="container-fluid">
  <table class="table">
    <thead>
    <tr>
      <th>Name</th>
      <th>Active</th>
      <th>Borrowed</th>
      <th>Item type</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td class="text-center"><%= item.active ? '✅' : '🚫' %></td>
        <td class="text-center"><%= item.borrowed ? '✅' : '🚫' %></td>
        <td><%= item.item_type %></td>
        <td>
          <%= link_to "Show this item", item, class: 'btn btn-primary' %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

</div>


