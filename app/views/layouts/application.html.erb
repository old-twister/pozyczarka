<!DOCTYPE html>
<html lang="pl">
  <head>
    <title>Pozyczarka</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body class="container">
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <p class="navbar-brand">Pożyczarka</p>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <%= link_to 'All', root_path, class: ['nav-link', ('active' if current_page?(root_path))] %>
          <%= link_to 'Active', active_items_path, class: ['nav-link', ('active' if current_page?(active_items_path))] %>
          <%= link_to 'Inactive', inactive_items_path, class: ['nav-link', ('active' if current_page?(inactive_items_path))] %>
          <%= link_to 'Borrowed', borrowed_items_path, class: ['nav-link', ('active' if current_page?(borrowed_items_path))] %>
          <%= link_to 'On place', on_place_items_path, class: ['nav-link', ('active' if current_page?(on_place_items_path))] %>
        </div>
      </div>
    </div>
  </nav>
    <%= yield %>
  </body>
</html>
